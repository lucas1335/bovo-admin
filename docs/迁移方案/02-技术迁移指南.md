# Vue到React技术迁移指南

## 一、核心概念映射

### 1.1 数据状态映射

| Vue | React | 说明 |
|-----|-------|------|
| `data()` | `useState` | 组件状态 |
| `computed` | `useMemo` | 计算属性 |
| `methods` | 函数声明 | 方法函数 |
| `watch` | `useEffect` | 监听器 |
| `mounted()` | `useEffect(() => {}, [])` | 挂载后 |
| `beforeDestroy()` | `useEffect(() => { return cleanup }, [])` | 销毁前 |
| `props` | `props` 参数 | 组件属性 |
| `$emit` | 回调函数props | 事件发射 |
| `$refs` | `useRef` | 引用 |
| `provide/inject` | Context | 依赖注入 |

### 1.2 指令转换

| Vue指令 | React实现 | 示例 |
|---------|-----------|------|
| `v-if` | 条件渲染 | `{condition && <Component />}` |
| `v-show` | style显示 | `{style: { display: condition ? 'block' : 'none' }}` |
| `v-for` | map渲染 | `{list.map(item => <Item key={item.id} />)}` |
| `v-model` | value+onChange | `<Input value={val} onChange={setVal} />` |
| `v-bind` | {} | `<div className={classVar} />` |
| `v-on` | on事件 | `<Button onClick={handleClick} />` |
| `v-html` | dangerouslySetInnerHTML | `<div dangerouslySetInnerHTML={{__html: html}} />` |

## 二、组件迁移模板

### 2.1 基础列表页面迁移

#### Vue源模板（用户管理列表）
```vue
<template>
  <div class="app-container">
    <!-- 搜索表单 -->
    <el-form v-show="showSearch" :model="queryParams">
      <el-form-item label="用户名称" prop="userName">
        <el-input v-model="queryParams.userName" placeholder="请输入用户名称" />
      </el-form-item>
      <el-form-item>
        <el-button type="primary" @click="handleQuery">搜索</el-button>
        <el-button @click="resetQuery">重置</el-button>
      </el-form-item>
    </el-form>

    <!-- 工具栏 -->
    <el-row :gutter="10">
      <el-col :span="1.5">
        <el-button type="primary" @click="handleAdd">新增</el-button>
      </el-col>
    </el-row>

    <!-- 数据表格 -->
    <el-table v-loading="loading" :data="userList">
      <el-table-column label="用户编号" prop="userId" />
      <el-table-column label="用户名称" prop="userName" />
      <el-table-column label="状态" prop="status">
        <template slot-scope="scope">
          <el-tag v-if="scope.row.status === '0'">正常</el-tag>
          <el-tag v-else>停用</el-tag>
        </template>
      </el-table-column>
      <el-table-column label="操作">
        <template slot-scope="scope">
          <el-button size="mini" @click="handleUpdate(scope.row)">修改</el-button>
          <el-button size="mini" type="danger" @click="handleDelete(scope.row)">删除</el-button>
        </template>
      </el-table-column>
    </el-table>

    <!-- 分页 -->
    <pagination
      v-show="total>0"
      :total="total"
      :page.sync="queryParams.pageNum"
      :limit.sync="queryParams.pageSize"
      @pagination="getList"
    />

    <!-- 添加/修改对话框 -->
    <el-dialog :title="title" :visible.sync="open" width="600px">
      <el-form ref="form" :model="form" :rules="rules">
        <el-form-item label="用户名称" prop="userName">
          <el-input v-model="form.userName" placeholder="请输入用户名称" />
        </el-form-item>
      </el-form>
      <div slot="footer" class="dialog-footer">
        <el-button @click="cancel">取 消</el-button>
        <el-button type="primary" @click="submitForm">确 定</el-button>
      </div>
    </el-dialog>
  </div>
</template>

<script>
import { listUser, getUser, delUser, addUser, updateUser } from "@/api/system/user";

export default {
  name: "User",
  data() {
    return {
      // 遮罩层
      loading: true,
      // 选中数组
      ids: [],
      // 非单个禁用
      single: true,
      // 非多个禁用
      multiple: true,
      // 显示搜索条件
      showSearch: true,
      // 总条数
      total: 0,
      // 用户表格数据
      userList: [],
      // 弹出层标题
      title: "",
      // 是否显示弹出层
      open: false,
      // 查询参数
      queryParams: {
        pageNum: 1,
        pageSize: 10,
        userName: undefined,
      },
      // 表单参数
      form: {},
      // 表单校验
      rules: {
        userName: [
          { required: true, message: "用户名称不能为空", trigger: "blur" }
        ]
      }
    };
  },
  created() {
    this.getList();
  },
  methods: {
    /** 查询用户列表 */
    getList() {
      this.loading = true;
      listUser(this.queryParams).then(response => {
        this.userList = response.rows;
        this.total = response.total;
        this.loading = false;
      });
    },
    // 取消按钮
    cancel() {
      this.open = false;
      this.reset();
    },
    // 表单重置
    reset() {
      this.form = {
        userId: undefined,
        userName: undefined,
      };
      this.resetForm("form");
    },
    /** 搜索按钮操作 */
    handleQuery() {
      this.queryParams.pageNum = 1;
      this.getList();
    },
    /** 重置按钮操作 */
    resetQuery() {
      this.resetForm("queryForm");
      this.handleQuery();
    },
    /** 新增按钮操作 */
    handleAdd() {
      this.reset();
      this.open = true;
      this.title = "添加用户";
    },
    /** 修改按钮操作 */
    handleUpdate(row) {
      this.reset();
      const userId = row.userId || this.ids;
      getUser(userId).then(response => {
        this.form = response.data;
        this.open = true;
        this.title = "修改用户";
      });
    },
    /** 提交按钮 */
    submitForm() {
      this.$refs["form"].validate(valid => {
        if (valid) {
          if (this.form.userId != undefined) {
            updateUser(this.form).then(response => {
              this.$modal.msgSuccess("修改成功");
              this.open = false;
              this.getList();
            });
          } else {
            addUser(this.form).then(response => {
              this.$modal.msgSuccess("新增成功");
              this.open = false;
              this.getList();
            });
          }
        }
      });
    },
    /** 删除按钮操作 */
    handleDelete(row) {
      const userIds = row.userId || this.ids;
      this.$modal.confirm('是否确认删除用户编号为"' + userIds + '"的数据项？').then(function() {
        return delUser(userIds);
      }).then(() => {
        this.getList();
        this.$modal.msgSuccess("删除成功");
      });
    }
  }
};
</script>
```

#### React目标模板
```jsx
import React, { useState } from 'react';
import { message } from 'antd';
import CmBasePage from '@components/CmBasePage';
import DataForm from '@components/DataForm';
import { getSysUserList, saveSysUser, updateSysUser, deleteSysUser } from '@api';

/**
 * 用户管理页面
 * 功能：用户列表、新增、编辑、删除
 */
const UserPage = () => {
  // 1. 状态管理
  const [formVisible, setFormVisible] = useState(false);
  const [editingRecord, setEditingRecord] = useState(null);
  const [loading, setLoading] = useState(false);

  // 2. 列配置
  const columns = [
    {
      title: '用户编号',
      dataIndex: 'userId',
      key: 'userId',
      width: 100,
    },
    {
      title: '用户名称',
      dataIndex: 'userName',
      key: 'userName',
      width: 150,
    },
    {
      title: '状态',
      dataIndex: 'status',
      key: 'status',
      width: 100,
      render: (status) => {
        const statusMap = {
          '0': { color: 'green', text: '正常' },
          '1': { color: 'orange', text: '停用' },
        };
        const statusInfo = statusMap[status] || { color: 'default', text: '未知' };
        return <Tag color={statusInfo.color}>{statusInfo.text}</Tag>;
      },
    },
  ];

  // 3. 搜索字段配置
  const searchFieldList = [
    {
      title: '用户名称',
      dataIndex: 'userName',
      key: 'SEARCH_LIKE_userName',
      type: 'text',
    },
  ];

  // 4. 事件处理
  const handleAdd = () => {
    setEditingRecord(null);
    setFormVisible(true);
  };

  const handleEdit = (record) => {
    setEditingRecord(record);
    setFormVisible(true);
  };

  const handleDelete = async (record) => {
    try {
      const response = await deleteSysUser({ id: record.userId });
      if (response.code === 0 || response.code === 200) {
        message.success('删除成功');
        refreshTable();
      } else {
        message.error(response.msg || '删除失败');
      }
    } catch (error) {
      message.error('删除失败: ' + error.message);
    }
  };

  const handleSubmit = async (values) => {
    setLoading(true);
    try {
      const response = editingRecord
        ? await updateSysUser(values)
        : await saveSysUser(values);

      if (response.code === 0 || response.code === 200) {
        message.success(editingRecord ? '更新成功' : '创建成功');
        setFormVisible(false);
        refreshTable();
      } else {
        message.error(response.msg || '操作失败');
      }
    } catch (error) {
      message.error('操作失败: ' + error.message);
    } finally {
      setLoading(false);
    }
  };

  // 5. 刷新表格
  const refreshTable = () => {
    document.querySelector('.ant-pro-table')?.parentNode?.dispatchEvent(
      new CustomEvent('reload')
    );
  };

  // 6. 渲染
  return (
    <div>
      <CmBasePage
        columns={columns}
        apiEndpoint="/sysUsers/getList"
        apiMethod="post"
        searchFieldList={searchFieldList}
        onAdd={handleAdd}
        onEdit={handleEdit}
        onDelete={handleDelete}
        rowKey="userId"
      />

      <DataForm
        visible={formVisible}
        title={editingRecord ? '编辑用户' : '新增用户'}
        initialValues={editingRecord || {}}
        extraValues={{ userId: editingRecord?.userId }}
        onCancel={() => setFormVisible(false)}
        onSubmit={handleSubmit}
        onClosed={() => setEditingRecord(null)}
        loading={loading}
        formType="drawer"
      >
        <Form.Item
          name="userName"
          label="用户名称"
          rules={[{ required: true, message: '请输入用户名称' }]}
        >
          <Input placeholder="请输入用户名称" />
        </Form.Item>
      </DataForm>
    </div>
  );
};

export default UserPage;
```

### 2.2 带树形分类的页面迁移

#### Vue源模板（分类+列表）
```vue
<template>
  <div class="app-container">
    <!-- 左侧分类树 -->
    <div class="left-container">
      <el-tree
        :data="classifyOptions"
        :props="defaultProps"
        :expand-on-click-node="false"
        @node-click="handleNodeClick"
      />
    </div>

    <!-- 右侧内容区 -->
    <div class="right-container">
      <el-table :data="dataList">
        <!-- ... -->
      </el-table>
    </div>
  </div>
</template>

<script>
export default {
  data() {
    return {
      classifyOptions: [],
      dataList: [],
      queryParams: {
        classifyId: null
      }
    };
  },
  created() {
    this.getClassifyTree();
    this.getList();
  },
  methods: {
    getClassifyTree() {
      // 获取分类树
    },
    handleNodeClick(data) {
      this.queryParams.classifyId = data.id;
      this.getList();
    },
    getList() {
      // 获取列表数据
    }
  }
};
</script>
```

#### React目标模板
```jsx
import React, { useState, useRef, useEffect } from 'react';
import CmBasePage from '@components/CmBasePage';
import CategoryTree from './components/CategoryTree';

const PageName = () => {
  const [sidebarCollapsed, setSidebarCollapsed] = useState(false);
  const [selectedCategoryId, setSelectedCategoryId] = useState(null);

  // 使用ref存储最新分类ID，避免闭包问题
  const categoryIdRef = useRef(null);

  useEffect(() => {
    categoryIdRef.current = selectedCategoryId;
  }, [selectedCategoryId]);

  // 自定义数据加载函数
  const loadData = async (params, sorter, filter) => {
    const currentCategoryId = categoryIdRef.current;

    // 添加分类筛选
    if (currentCategoryId) {
      const searchParam = params.searchParam
        ? JSON.parse(params.searchParam)
        : {};
      searchParam.SEARCH_EQ_categoryId = currentCategoryId;
      params.searchParam = JSON.stringify(searchParam);
    }

    // 调用API获取数据
    const response = await getDataList(params);
    return {
      data: response.data?.records || [],
      success: true,
      total: response.data?.total || 0
    };
  };

  const handleCategorySelect = (categoryId) => {
    setSelectedCategoryId(categoryId);
    // 触发表格刷新
    setTimeout(() => {
      document.querySelector('.ant-pro-table')?.parentNode?.dispatchEvent(
        new CustomEvent('reload')
      );
    }, 0);
  };

  const handleToggleSidebar = () => {
    setSidebarCollapsed(!sidebarCollapsed);
  };

  return (
    <div className="page-container">
      {/* 左侧分类树 */}
      {!sidebarCollapsed && (
        <div className="page-sidebar">
          <CategoryTree
            value={selectedCategoryId}
            onSelect={handleCategorySelect}
          />
        </div>
      )}

      {/* 展开/收起按钮 */}
      <div
        className="sidebar-toggle-btn"
        onClick={handleToggleSidebar}
        style={{ left: sidebarCollapsed ? 0 : 280 }}
      >
        {sidebarCollapsed ? '»' : '«'}
      </div>

      {/* 右侧内容区 */}
      <div className="page-content">
        <CmBasePage
          columns={columns}
          onLoadData={loadData}
          rowKey="id"
        />
      </div>
    </div>
  );
};

export default PageName;
```

## 三、UI组件迁移对照表

### 3.1 表单组件

| Element UI | Ant Design | 迁移说明 |
|------------|------------|----------|
| `<el-input>` | `<Input>` | 直接替换 |
| `<el-input type="textarea">` | `<Input.TextArea>` | 类型变化 |
| `<el-input-number>` | `<InputNumber>` | 组件名变化 |
| `<el-select>` | `<Select>` | 选项结构变化 |
| `<el-option>` | `<Select.Option>` | 选项结构变化 |
| `<el-cascader>` | `<Cascader>` | 直接替换 |
| `<el-switch>` | `<Switch>` | 直接替换 |
| `<el-radio>` | `<Radio>` | 单选用法不同 |
| `<el-radio-group>` | `<Radio.Group>` | 组包装方式 |
| `<el-checkbox>` | `<Checkbox>` | 复选用法不同 |
| `<el-checkbox-group>` | `<Checkbox.Group>` | 组包装方式 |
| `<el-date-picker>` | `<DatePicker>` | 日期选择 |
| `<el-time-picker>` | `<TimePicker>` | 时间选择 |
| `<el-upload>` | `<Upload>` | 上传参数结构不同 |
| `<el-rate>` | `<Rate>` | 直接替换 |
| `<el-slider>` | `<Slider>` | 直接替换 |
| `<el-transfer>` | `<Transfer>` | 直接替换 |

### 3.2 数据展示组件

| Element UI | Ant Design | 迁移说明 |
|------------|------------|----------|
| `<el-table>` | `<Table>` | columns配置不同 |
| `<el-tree>` | `<Tree>` | 数据结构可能需要调整 |
| `<el-pagination>` | 内置于Table | 分页集成方式不同 |
| `<el-badge>` | `<Badge>` | 直接替换 |
| `<el-avatar>` | `<Avatar>` | 直接替换 |
| `<el-tag>` | `<Tag>` | 直接替换 |
| `<el-progress>` | `<Progress>` | 直接替换 |
| `<el-card>` | `<Card>` | 直接替换 |
| `<el-collapse>` | `<Collapse>` | 结构略有不同 |
| `<el-timeline>` | `<Timeline>` | 直接替换 |
| `<el-descriptions>` | `<Descriptions>` | 直接替换 |
| `<el-empty>` | `<Empty>` | 直接替换 |

### 3.3 导航组件

| Element UI | Ant Design | 迁移说明 |
|------------|------------|----------|
| `<el-menu>` | `<Menu>` | 菜单项结构不同 |
| `<el-submenu>` | `<Menu.SubMenu>` | 结构变化 |
| `<el-menu-item>` | `<Menu.Item>` | 结构变化 |
| `<el-tabs>` | `<Tabs>` | TabPane结构不同 |
| `<el-tab-pane>` | `<Tabs.TabPane>` | 结构变化 |
| `<el-breadcrumb>` | `<Breadcrumb>` | 结构略有不同 |
| `<el-dropdown>` | `<Dropdown>` | 菜单结构不同 |
| `<el-steps>` | `<Steps>` | 直接替换 |

### 3.4 反馈组件

| Element UI | Ant Design | 迁移说明 |
|------------|------------|----------|
| `<el-alert>` | `<Alert>` | 直接替换 |
| `<el-loading>` | `<spin>` | 用法不同 |
| `<el-message>` | `message` | 调用方式不同 |
| `<el-notification>` | `notification` | 调用方式不同 |
| `<el-dialog>` | `<Modal>` | 属性名称不同 |
| `<el-drawer>` | `<Drawer>` | 直接替换 |
| `<el-popconfirm>` | `<Popconfirm>` | 直接替换 |
| `<el-popover>` | `<Popover>` | 直接替换 |
| `<el-tooltip>` | `<Tooltip>` | 直接替换 |

## 四、常用场景迁移

### 4.1 表格操作列

#### Vue
```vue
<el-table-column label="操作" align="center" class-name="small-padding fixed-width">
  <template slot-scope="scope">
    <el-button
      size="mini"
      type="text"
      icon="el-icon-edit"
      @click="handleUpdate(scope.row)"
    >修改</el-button>
    <el-button
      size="mini"
      type="text"
      icon="el-icon-delete"
      @click="handleDelete(scope.row)"
    >删除</el-button>
  </template>
</el-table-column>
```

#### React
```jsx
{
  title: '操作',
  key: 'action',
  fixed: 'right',
  width: 180,
  render: (_, record) => (
    <Space>
      <Button type="link" size="small" onClick={() => handleEdit(record)}>
        编辑
      </Button>
      <Button type="link" size="small" danger onClick={() => handleDelete(record)}>
        删除
      </Button>
    </Space>
  ),
}
```

### 4.2 表单验证

#### Vue
```javascript
rules: {
  userName: [
    { required: true, message: '请输入用户名称', trigger: 'blur' },
    { min: 2, max: 20, message: '长度在 2 到 20 个字符', trigger: 'blur' }
  ],
  email: [
    { type: 'email', message: '请输入正确的邮箱地址', trigger: ['blur', 'change'] }
  ]
}
```

#### React
```jsx
<Form.Item
  name="userName"
  label="用户名称"
  rules={[
    { required: true, message: '请输入用户名称' },
    { min: 2, max: 20, message: '长度在 2 到 20 个字符' }
  ]}
>
  <Input placeholder="请输入用户名称" />
</Form.Item>

<Form.Item
  name="email"
  label="邮箱"
  rules={[
    { type: 'email', message: '请输入正确的邮箱地址' }
  ]}
>
  <Input placeholder="请输入邮箱" />
</Form.Item>
```

### 4.3 日期范围选择

#### Vue
```vue
<el-form-item label="创建时间">
  <el-date-picker
    v-model="dateRange"
    style="width: 240px"
    value-format="yyyy-MM-dd"
    type="daterange"
    range-separator="-"
    start-placeholder="开始日期"
    end-placeholder="结束日期"
  />
</el-form-item>
```

#### React
```jsx
<Form.Item label="创建时间">
  <DatePicker.RangePicker
    style={{ width: '100%' }}
    format="YYYY-MM-DD"
    placeholder={['开始日期', '结束日期']}
  />
</Form.Item>
```

### 4.4 图片上传

#### Vue
```vue
<el-upload
  class="avatar-uploader"
  :action="uploadUrl"
  :show-file-list="false"
  :on-success="handleAvatarSuccess"
  :before-upload="beforeAvatarUpload"
>
  <img v-if="imageUrl" :src="imageUrl" class="avatar">
  <i v-else class="el-icon-plus avatar-uploader-icon"></i>
</el-upload>
```

#### React
```jsx
<Form.Item
  name="avatar"
  label="头像"
  rules={[{ required: true, message: '请上传头像' }]}
>
  <CmUpload />
</Form.Item>
```

### 4.5 状态显示

#### Vue
```vue
<el-tag v-if="scope.row.status === '0'" type="success">正常</el-tag>
<el-tag v-else-if="scope.row.status === '1'" type="warning">停用</el-tag>
<el-tag v-else type="danger">删除</el-tag>
```

#### React
```jsx
{
  title: '状态',
  dataIndex: 'status',
  key: 'status',
  render: (status) => {
    const statusMap = {
      '0': { color: 'green', text: '正常' },
      '1': { color: 'orange', text: '停用' },
      '2': { color: 'red', text: '删除' },
    };
    const statusInfo = statusMap[status] || { color: 'default', text: '未知' };
    return <Tag color={statusInfo.color}>{statusInfo.text}</Tag>;
  },
}
```

## 五、API调用迁移

### 5.1 API定义迁移

#### Vue项目结构
```javascript
// src/api/system/user.js
import request from '@/utils/request'

// 查询用户列表
export function listUser(query) {
  return request({
    url: '/system/user/list',
    method: 'get',
    params: query
  })
}

// 新增用户
export function addUser(data) {
  return request({
    url: '/system/user',
    method: 'post',
    data: data
  })
}
```

#### React项目结构
```javascript
// src/api/modules/system.js
import { apiService } from '../base';

/**
 * 查询用户列表
 */
export const getSysUserList = (params) => {
  return apiService.post('/sysUsers/getList', params);
};

/**
 * 新增用户
 */
export const saveSysUser = (data) => {
  return apiService.post('/sysUsers/save', data);
};

/**
 * 更新用户
 */
export const updateSysUser = (data) => {
  return apiService.post('/sysUsers/update', data);
};

/**
 * 删除用户
 */
export const deleteSysUser = (params) => {
  return apiService.post('/sysUsers/delete', params);
};
```

### 5.2 搜索参数迁移

#### Vue项目
```javascript
const queryParams = {
  pageNum: 1,
  pageSize: 10,
  userName: 'admin',
  status: '0'
};

// 直接传递
listUser(queryParams);
```

#### React项目
```javascript
// 需要转换为searchParam JSON字符串
const params = {
  pageNum: 1,
  pageSize: 10,
  searchParam: JSON.stringify({
    SEARCH_LIKE_userName: 'admin',
    SEARCH_EQ_status: '0'
  })
};

getSysUserList(params);
```

## 六、样式迁移

### 6.1 Scoped样式

#### Vue
```vue
<style scoped>
.user-container {
  padding: 20px;
}
.user-table {
  margin-top: 20px;
}
</style>
```

#### React（CSS Modules）
```css
/* UserPage.module.css */
.userContainer {
  padding: 20px;
}
.userTable {
  margin-top: 20px;
}
```

```jsx
import styles from './UserPage.module.css';

<div className={styles.userContainer}>
  <Table className={styles.userTable} />
</div>
```

#### React（普通CSS）
```css
/* UserPage.css */
.page-container {
  padding: 20px;
}
.table-container {
  margin-top: 20px;
}
```

```jsx
import './UserPage.css';

<div className="page-container">
  <Table className="table-container" />
</div>
```

## 七、路由迁移

### 7.1 路由定义

#### Vue Router
```javascript
{
  path: '/system/user',
  component: Layout,
  children: [
    {
      path: '',
      name: 'User',
      component: () => import('@/views/system/user/index'),
      meta: { title: '用户管理', icon: 'user' }
    }
  ]
}
```

#### React Router
```javascript
{
  key: 'user-management',
  path: '/cm-system/user',
  component: lazy(() => import('../pages/cm-system/user.jsx')),
}

// 或使用动态路由（无需手动注册）
// 访问 /cm-system/user 自动加载 ../pages/cm-system/user.jsx
```

### 7.2 路由跳转

#### Vue
```javascript
// 声明式
<router-link :to="'/system/user'">用户管理</router-link>

// 编程式
this.$router.push('/system/user');
this.$router.push({ path: '/system/user', query: { id: 1 }});
```

#### React
```jsx
// 声明式
import { Link } from 'react-router-dom';
<Link to="/cm-system/user">用户管理</Link>

// 编程式
import { useNavigate } from 'react-router-dom';
const navigate = useNavigate();
navigate('/cm-system/user');
navigate('/cm-system/user', { state: { id: 1 }});
```

### 7.3 路由参数获取

#### Vue
```javascript
// query参数
this.$route.query.id

// path参数
this.$route.params.id
```

#### React
```jsx
// query参数
import { useSearchParams } from 'react-router-dom';
const [searchParams] = useSearchParams();
const id = searchParams.get('id');

// path参数
import { useParams } from 'react-router-dom';
const { id } = useParams();
```

## 八、常见问题处理

### 8.1 v-model转换

#### Vue
```vue
<el-input v-model="form.userName" />
```

#### React
```jsx
const [userName, setUserName] = useState('');

<Input
  value={userName}
  onChange={(e) => setUserName(e.target.value)}
/>

// 或使用Form.Item的name属性
<Form.Item name="userName">
  <Input />
</Form.Item>
```

### 8.2 事件修饰符

| Vue事件修饰符 | React实现 |
|--------------|-----------|
| `.stop` | `e.stopPropagation()` |
| `.prevent` | `e.preventDefault()` |
| `.native` | 直接在原生元素上绑定 |
| `.once` | 使用useState控制 |
| `.left/right/middle` | `e.button === 0/1/2` |

### 8.3 按钮权限

#### Vue
```vue
<el-button v-hasPermi="['system:user:add']">新增</el-button>
```

#### React
```jsx
// 使用上下文权限
import { useAuth } from '@contexts/AuthContext';

const { hasPermission } = useAuth();

{hasPermission('system:user:add') && (
  <Button>新增</Button>
)}
```

## 九、迁移检查清单

在完成每个页面迁移后，请确认：

- [ ] 页面布局正确
- [ ] 列表数据正常显示
- [ ] 搜索功能正常
- [ ] 分页功能正常
- [ ] 新增功能正常
- [ ] 编辑功能正常
- [ ] 删除功能正常
- [ ] 表单验证正常
- [ ] 权限控制正常
- [ ] 样式显示正确
